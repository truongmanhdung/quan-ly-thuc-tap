import React from 'react';

const RequestFromStudent = (props) => {
    const columns = [
        {
            title: 'STT',
        },
        
    ]
  return <div>
    <span>fsdfs</span>
  </div>;
};

RequestFromStudent.propTypes = {};

export default RequestFromStudent;
